<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyExpenses.Smartphones.ContentPages.Analytics.AccountsCategorySumPositiveNegativeContent.AccountsCategorySumPositiveNegativeContentPage"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:local="clr-namespace:MyExpenses.Smartphones.ContentPages.Analytics.AccountsCategorySumPositiveNegativeContent"

             Title="{Binding AccountsCategorySumPositiveNegativeContentPageTitle, Mode=OneWay}"
             BindingContext="{Binding Source={RelativeSource Self}}">

    <ContentPage.Content>

        <material:TabView ItemsSource="{Binding TabItemDatas, Mode=OneWay}"
                          TabPlacement="Bottom">

            <material:TabView.ItemTemplate>
                <DataTemplate>
                    <local:AccountCategorySumPositiveNegativeContentView />
                </DataTemplate>
            </material:TabView.ItemTemplate>

        </material:TabView>

    </ContentPage.Content>

</ContentPage>