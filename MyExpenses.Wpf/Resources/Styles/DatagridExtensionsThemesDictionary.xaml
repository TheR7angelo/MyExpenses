<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
                    xmlns:dgx="urn:tom-englert.de/DataGridExtensions"
                    xmlns:toms="urn:TomsToolbox"
                    xmlns:System="clr-namespace:System;assembly=System.Runtime"
                    xmlns:localisations="clr-namespace:MyExpenses.Wpf.Localisations"
                    xmlns:dashboardResources="clr-namespace:MyExpenses.Presentation.Resources.Resx.DashBoardResources;assembly=MyExpenses.Presentation">

    <!-- Style for a filter check box. The check box is only visible when it's hovered, focused or has a value.  -->
    <Style x:Key="{x:Static dgx:DataGridFilter.ColumnHeaderSearchCheckBoxStyleKey}" TargetType="CheckBox"
           BasedOn="{StaticResource {x:Type CheckBox}}">
        <Setter Property="IsThreeState" Value="True" />
        <Setter Property="Margin" Value="4,0,2,0" />
        <Style.Triggers>
            <Trigger Property="CheckBox.IsChecked" Value="{x:Null}">
                <Setter Property="Opacity" Value="0" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Style for the clear button in the filter text box -->
    <Style x:Key="{x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxClearButtonStyleKey}" TargetType="Button">
        <Setter Property="Foreground" Value="LightGray" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ButtonBase">
                    <Border Background="Transparent">
                        <Path Data="M0,0 L7,7 M0,7 L7,0" Stroke="{TemplateBinding Foreground}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" StrokeThickness="2" Margin="3" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static dgx:DataGridFilter.SearchTextBoxStyleKey}" TargetType="{x:Type TextBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <DockPanel>
                            <Button x:Name="clear" DockPanel.Dock="Right" Focusable="False"
                                    Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxClearButtonStyleKey}}">
                                <i:Interaction.Behaviors>
                                    <dgx:ClearTextBoxOnButtonClickBehavior
                                        TextBox="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                                </i:Interaction.Behaviors>
                            </Button>
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="border" Value="0.56" />
                        </Trigger>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Visibility" TargetName="clear" Value="Hidden" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style for a filter text box. The text box is only visible when it's hovered, focused or has a value.  -->
    <Style x:Key="{x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxStyleKey}" TargetType="TextBox">
        <Setter Property="MinWidth" Value="20" />
        <Setter Property="Margin" Value="4,0,2,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <DockPanel>
                            <Button x:Name="clear" DockPanel.Dock="Right" Focusable="False"
                                    Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxClearButtonStyleKey}}">
                                <i:Interaction.Behaviors>
                                    <dgx:ClearTextBoxOnButtonClickBehavior
                                        TextBox="{Binding RelativeSource={RelativeSource TemplatedParent}}" />
                                </i:Interaction.Behaviors>
                            </Button>
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="border" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF7EB4EA" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF569DE5" />
                        </Trigger>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Visibility" TargetName="clear" Value="Hidden" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Text" Value="">
                <Setter Property="Opacity" Value="0" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Reusable filter symbol -->
    <ControlTemplate x:Key="{x:Static dgx:DataGridFilter.IconTemplateKey}">
        <Path Data="M0,0 L10,0 6,4 6,9 4,9 4,4 Z" Fill="{TemplateBinding Foreground}" VerticalAlignment="Center"
              HorizontalAlignment="Right" Margin="2,0" />
    </ControlTemplate>

    <Style TargetType="Control" x:Key="{x:Static dgx:DataGridFilter.IconStyleKey}">
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template" Value="{DynamicResource {x:Static dgx:DataGridFilter.IconTemplateKey}}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="Gray" />
    </Style>

    <!-- Default template for a filter on a column represented by a DataGridTextColumn. -->
    <ControlTemplate x:Key="{x:Static dgx:DataGridFilter.TextColumnFilterTemplateKey}">
        <Grid>
            <Control Style="{DynamicResource {x:Static dgx:DataGridFilter.IconStyleKey}}" />
            <TextBox Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxStyleKey}}"
                     Text="{Binding Path=Filter, UpdateSourceTrigger=PropertyChanged}"
                     dgx:Tools.MoveFocusToDataGridOnNavigationKey="true" />
        </Grid>
    </ControlTemplate>

    <!-- Default template for a filter on a column represented by a DataGridCheckBoxColumn. -->
    <ControlTemplate x:Key="{x:Static dgx:DataGridFilter.CheckBoxColumnFilterTemplateKey}">
        <Grid>
            <Control Style="{DynamicResource {x:Static dgx:DataGridFilter.IconStyleKey}}" />
            <CheckBox Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchCheckBoxStyleKey}}"
                      IsChecked="{Binding Path=Filter, UpdateSourceTrigger=PropertyChanged}"
                      dgx:Tools.MoveFocusToDataGridOnNavigationKey="true" />
        </Grid>
    </ControlTemplate>

    <!-- Default template for a filter on a column represented by a DataGridTemplateColumn. -->
    <ControlTemplate x:Key="{x:Static dgx:DataGridFilter.TemplateColumnFilterTemplateKey}">
        <Grid>
            <Control Style="{DynamicResource {x:Static dgx:DataGridFilter.IconStyleKey}}" />
            <TextBox Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxStyleKey}}"
                     Text="{Binding Path=Filter, UpdateSourceTrigger=PropertyChanged}"
                     dgx:Tools.MoveFocusToDataGridOnNavigationKey="true" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="{x:Static dgx:DataGridFilter.ComboBoxColumnFilterTemplateKey}">
        <Grid>
            <Control Style="{DynamicResource {x:Static dgx:DataGridFilter.IconStyleKey}}" />
            <TextBox Style="{DynamicResource {x:Static dgx:DataGridFilter.ColumnHeaderSearchTextBoxStyleKey}}"
                     Text="{Binding Path=Filter, UpdateSourceTrigger=PropertyChanged}"
                     dgx:Tools.MoveFocusToDataGridOnNavigationKey="true" />
        </Grid>
    </ControlTemplate>

    <!-- Default template for the column header including the filter control. -->
    <DataTemplate x:Key="{x:Static dgx:DataGridFilter.ColumnHeaderTemplateKey}">
        <DockPanel Background="Transparent">
            <!-- The control to host the filter UI for this column -->
            <dgx:DataGridFilterColumnControl DockPanel.Dock="Right" />
            <ContentPresenter x:Name="PART_Content" Content="{Binding}"
                              ContentStringFormat="{Binding Column.HeaderStringFormat, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridColumnHeader}}"
                              SnapsToDevicePixels="{Binding SnapsToDevicePixels, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridColumnHeader}}"
                              HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridColumnHeader}}"
                              VerticalAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=DataGridColumnHeader}}" />
        </DockPanel>
    </DataTemplate>

    <Style TargetType="ToolTip" x:Key="{x:Static dgx:ExtendedStarSizeBehavior.ColumnHeaderGripperToolTipStyleKey}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="+Ctrl => Apply star sizes to right columns" /><LineBreak />
                        <Run Text="+Shift => Extend beyond available size" />
                    </TextBlock>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type dgx:MultipleChoiceFilter}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type dgx:MultipleChoiceFilter}">
                    <StackPanel Orientation="Horizontal">
                        <Decorator Width="5" />
                        <ToggleButton x:Name="ToggleButton"
                                      Background="Transparent"
                                      BorderBrush="Transparent"
                                      BorderThickness="0"
                                      Cursor="Hand"
                                      ClickMode="Press" Focusable="False"
                                      IsChecked="{Binding IsPopupOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Style="{StaticResource {x:Type ToggleButton}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent" Padding="4,2">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock x:Name="IsFilterActiveMarker"
                                                       Visibility="Hidden"
                                                       Text="•" Margin="0,0,2,0"
                                                       FontWeight="Bold"
                                                       VerticalAlignment="Center" />

                                            <Control x:Name="FilterSymbol"
                                                     Style="{DynamicResource {x:Static dgx:DataGridFilter.IconStyleKey}}" />
                                        </StackPanel>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="FilterSymbol" Property="Foreground"
                                                    Value="{DynamicResource PrimaryHueMidBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="FilterSymbol" Property="Foreground"
                                                    Value="{DynamicResource MaterialDesignBody}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="Popup" StaysOpen="False"
                               IsOpen="{Binding IsPopupOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                               AllowsTransparency="True">
                            <FrameworkElement.Resources>
                                <CollectionViewSource x:Key="SortedValuesSource"
                                                      Source="{Binding Values, RelativeSource={RelativeSource TemplatedParent}}">
                                    <CollectionViewSource.SortDescriptions>
                                        <componentModel:SortDescription />
                                    </CollectionViewSource.SortDescriptions>
                                </CollectionViewSource>
                            </FrameworkElement.Resources>
                            <i:Interaction.Behaviors>
                                <toms:PopupFocusManagerBehavior ToggleButton="{Binding ElementName=ToggleButton}" />
                            </i:Interaction.Behaviors>
                            <Border Background="{DynamicResource MaterialDesignPaper}"
                                    BorderThickness="1"
                                    MinWidth="100"
                                    BorderBrush="{DynamicResource MaterialDesignBody}"
                                    CornerRadius="3">
                                <DockPanel KeyboardNavigation.DirectionalNavigation="Cycle">
                                    <StackPanel DockPanel.Dock="Top">
                                        <StackPanel x:Name="CompositeTextBox" Visibility="Collapsed">
                                            <TextBox dgx:Tools.MoveFocusOnNavigationKey="True"
                                                     BorderThickness="0"
                                                     Margin="3"
                                                     Style="{DynamicResource {x:Static dgx:DataGridFilter.SearchTextBoxStyleKey}}"
                                                     Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}" />
                                            <Border BorderThickness="0,1,0,0"
                                                    BorderBrush="{DynamicResource MaterialDesignBody}" />
                                        </StackPanel>
                                        <CheckBox x:Name="SelectAll"
                                                  IsThreeState="True"
                                                  Content="{localisations:Tr {x:Static dashboardResources:DashBoardResourceManager.DataGridFilterSelectAll}}"
                                                  Margin="4,3,14,3"
                                                  Style="{DynamicResource {x:Type CheckBox}}" />
                                        <Border BorderThickness="0,1,0,0"
                                                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" />
                                    </StackPanel>
                                    <ListBox x:Name="PART_ListBox"
                                             KeyboardNavigation.DirectionalNavigation="Continue"
                                             ItemsSource="{Binding Source={StaticResource SortedValuesSource}}"
                                             Style="{DynamicResource {x:Type ListBox}}"
                                             SelectionMode="Multiple"
                                             BorderThickness="0"
                                             Margin="1"
                                             Background="Transparent"
                                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="ListBoxItem">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <DockPanel>
                                                                <CheckBox DockPanel.Dock="Left"
                                                                          Margin="3,2"
                                                                          IsChecked="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}}"
                                                                          Style="{DynamicResource {x:Type CheckBox}}" />
                                                                <Decorator DockPanel.Dock="Right"
                                                                           Width="10" />
                                                                <ContentPresenter VerticalAlignment="Center" />
                                                            </DockPanel>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock x:Name="Text" Margin="3,2" Text="{Binding}"
                                                           VerticalAlignment="Center" />
                                                <DataTemplate.Triggers>

                                                    <DataTrigger Binding="{Binding}" Value="{x:Static System:Boolean.TrueString}">
                                                        <Setter TargetName="Text" Property="Text" Value="{localisations:Tr {x:Static dashboardResources:DashBoardResourceManager.DataGridFilterYes}}" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding}" Value="{x:Static System:Boolean.FalseString}">
                                                        <Setter TargetName="Text" Property="Text" Value="{localisations:Tr {x:Static dashboardResources:DashBoardResourceManager.DataGridFilterNo}}" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding}" Value="">
                                                        <Setter TargetName="Text" Property="Text" Value="(Empty)" />
                                                    </DataTrigger>

                                                    <DataTrigger Binding="{Binding}" Value="{x:Null}" />

                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <i:Interaction.Behaviors>
                                            <toms:ListBoxSelectAllBehavior
                                                AreAllFilesSelected="{Binding IsChecked, ElementName=SelectAll}" />
                                        </i:Interaction.Behaviors>
                                    </ListBox>
                                </DockPanel>
                            </Border>
                        </Popup>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="False">
                            <Setter TargetName="ToggleButton"
                                    Property="BorderBrush"
                                    Value="Transparent" />
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=Filter.IsFiltered, RelativeSource={RelativeSource Self}}"
                                     Value="True">
                            <!-- <Setter TargetName="IsFilterActiveMarker" -->
                            <!--         Property="Visibility" -->
                            <!--         Value="Visible" /> -->
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=HasTextFilter, RelativeSource={RelativeSource Self}}"
                                     Value="true">
                            <Setter TargetName="CompositeTextBox"
                                    Property="Visibility"
                                    Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>