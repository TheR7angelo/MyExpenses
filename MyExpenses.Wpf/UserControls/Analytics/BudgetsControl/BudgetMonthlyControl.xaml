<UserControl x:Class="MyExpenses.Wpf.UserControls.Analytics.BudgetsControl.BudgetMonthlyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MyExpenses.Wpf.UserControls.Analytics.BudgetsControl"
             xmlns:wpf="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"

             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ItemsControl Grid.Row="0" HorizontalAlignment="Center"
                      ItemsSource="{Binding CheckBoxes, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

        </ItemsControl>

        <ItemsControl Grid.Row="1" HorizontalAlignment="Center"
                      ItemsSource="{Binding CheckBoxesTrend, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

        </ItemsControl>

        <wpf:CartesianChart Grid.Row="2"
                            Series="{Binding Series, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}"
                            XAxes="{Binding XAxis, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}"
                            YAxes="{Binding YAxis, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}"
                            LegendTextPaint="{Binding TextPaint, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType={x:Type local:BudgetMonthlyControl}}}"
                            LegendPosition="Bottom"
                            ZoomMode="X">
        </wpf:CartesianChart>

    </Grid>

</UserControl>
