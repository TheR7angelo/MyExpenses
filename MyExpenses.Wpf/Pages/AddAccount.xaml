<mah:MetroWindow x:Class="MyExpenses.Wpf.Pages.AddAccount"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:MyExpenses.Wpf.Pages"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 xmlns:utils="clr-namespace:MyExpenses.Utils;assembly=MyExpenses.Utils"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 mc:Ignorable="d"

                 Title="AddAccount"

                 d:DesignHeight="585"
                 d:DesignWidth="458"

                 MinHeight="585"
                 MinWidth="458"
                 Height="585"
                 Width="458"

                 TitleCharacterCasing="Normal"
                 NonActiveWindowTitleBrush="Gray"
                 WindowTitleBrush="Gray"
                 BorderBrush="Gray"
                 GlowBrush="Gray"
                 NonActiveGlowBrush="Gray"

                 ShowIconOnTitleBar="True"

                 WindowStartupLocation="CenterScreen"
                 ResizeMode="CanResizeWithGrip"

                 Background="{DynamicResource MaterialDesignPaper}"
                 TextElement.Foreground="{DynamicResource MaterialDesignBody}"

                 DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid utils:PanelExtensions.MarginForAllChildren="5"
          VerticalAlignment="Center"
          HorizontalAlignment="Center">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                 materialDesign:HintAssist.Hint="{Binding TextBoxAccountName}"
                 Style="{StaticResource MaterialDesignFilledTextBox}"
                 Text="{Binding Account.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 LostFocus="TextBoxAccountName_OnLostFocus">
        </TextBox>

        <ComboBox Grid.Row="1" Grid.Column="0"
                  materialDesign:HintAssist.Hint="{Binding ComboBoxAccountType}"
                  materialDesign:TextFieldAssist.HasClearButton="True"
                  ItemsSource="{Binding AccountTypes}"
                  Style="{StaticResource MaterialDesignFilledComboBox}"
                  DisplayMemberPath="{Binding DisplayMemberPathAccountType}"
                  SelectedValue="{Binding Account.AccountTypeFk}"
                  SelectedValuePath="{Binding SelectedValuePathAccountType}">
        </ComboBox>

        <Label Grid.Row="1" Grid.Column="1" Content="Add add function" />

        <ComboBox Grid.Row="2" Grid.Column="0"
                  materialDesign:HintAssist.Hint="{Binding ComboBoxAccountCurrency}"
                  materialDesign:TextFieldAssist.HasClearButton="True"
                  ItemsSource="{Binding Currencies}"
                  Style="{StaticResource MaterialDesignFilledComboBox}"
                  DisplayMemberPath="{Binding DisplayMemberPathCurrency}"
                  SelectedValue="{Binding Account.CurrencyFk}"
                  SelectedValuePath="{Binding SelectedValuePathCurrency}">
        </ComboBox>

        <Label Grid.Row="2" Grid.Column="1" Content="Add add function" />

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal">
            <Label Margin="0, 0, 1.5, 0" VerticalContentAlignment="Center" Content="{Binding LabelIsAccountActive}" />

            <ToggleButton Margin="1.5, 0, 0, 0"
                          IsThreeState="False"
                          IsChecked="{Binding Account.Active}"
                          materialDesign:ToggleButtonAssist.OnContent="{materialDesign:PackIcon Kind=CheckBold}"
                          Content="{materialDesign:PackIcon Kind=CloseThick}"
                          Style="{StaticResource MaterialDesignActionToggleButton}">
            </ToggleButton>
        </StackPanel>

        <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                utils:PanelExtensions.MarginForAllChildren="5"
                IsEnabled="{Binding EnableStartingBalance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                BorderBrush="{DynamicResource MaterialDesignBody}"
                BorderThickness="1.3"
                CornerRadius="3">

            <Grid utils:PanelExtensions.MarginForAllChildren="3">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                         materialDesign:HintAssist.Hint="{Binding TextBoxAccountStartingBalanceDescription}"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         Text="{Binding History.Description, Mode=TwoWay}">
                </TextBox>

                <TextBox Grid.Row="1" Grid.Column="0"
                         materialDesign:HintAssist.Hint="{Binding TextBoxAccountStartingBalance}"
                         Style="{StaticResource MaterialDesignFilledTextBox}"
                         Text="{Binding History.Value, Mode=TwoWay}"
                         PreviewTextInput="TextBoxStartingBalance_OnPreviewTextInput">
                </TextBox>

                <ComboBox Grid.Row="1" Grid.Column="1"
                          materialDesign:HintAssist.Hint="{Binding ComboBoxAccountCategoryType}"
                          materialDesign:TextFieldAssist.HasClearButton="True"
                          ItemsSource="{Binding  CategoryTypes}"
                          Style="{StaticResource MaterialDesignFilledComboBox}"
                          DisplayMemberPath="{Binding DisplayMemberPathCategoryType}"
                          SelectedValue="{Binding History.CategoryTypeFk}"
                          SelectedValuePath="{Binding SelectedValuePathCategoryType}">
                </ComboBox>

                <Label Grid.Row="1" Grid.Column="2" Content="Add add function" />

            </Grid>

        </Border>

        <ToggleButton Grid.Row="4" Grid.Column="1"
                      IsChecked="{Binding EnableStartingBalance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource MaterialDesignSwitchToggleButton}"
                      Margin="0, 8, 8, 0"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      VerticalContentAlignment="Top"
                      IsThreeState="False">
        </ToggleButton>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                    utils:PanelExtensions.MarginForAllChildren="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="Valid"
                    Style="{StaticResource MaterialDesignPaperButton}"
                    Click="ButtonValid_OnClick">
            </Button>

            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignPaperSecondaryButton}"
                    Click="ButtonCancel_OnClick">
            </Button>
        </StackPanel>

    </Grid>

</mah:MetroWindow>